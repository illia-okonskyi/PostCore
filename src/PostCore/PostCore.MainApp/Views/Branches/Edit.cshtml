@model PostCore.MainApp.ViewModels.Branches.EditViewModel
@{
    ViewData["Title"] =
        Model.EditorMode == PostCore.ViewUtils.EditorMode.Create
            ? "Create branch"
            : "Edit branch";
    Layout = "_MainLayout";
}
@{
    var saveButtonText = Model.EditorMode == PostCore.ViewUtils.EditorMode.Create
            ? "Create"
            : "Save";
}
<div asp-validation-summary="All" class="text-danger"></div>
<div class="card m-2">
    <h5 class="card-header">Branch info:</h5>
    <div class="card-body">
        <form asp-action="Edit" method="post">
            <input asp-for="Id" />
            <input asp-for="EditorMode" />
            <input asp-for="ReturnUrl" />
            <div class="form-group">
                <label asp-for="Name"></label>
                <div><span asp-validation-for="Name" class="text-danger"></span></div>
                <input asp-for="Name" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="Address"></label>
                <div><span asp-validation-for="Address" class="text-danger"></span></div>
                <input asp-for="Address" class="form-control" />
            </div>
            <div class="text-center">
                <button class="btn btn-primary" type="submit">@saveButtonText</button>
                <a href="@Model.ReturnUrl" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>